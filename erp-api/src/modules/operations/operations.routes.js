"use strict";

// Placeholder do módulo Operations.
// Não expõe endpoints nesta fase (v1.0.0). A estrutura existe para crescer.

const { Router } = require("express");
const router = Router();

module.exports = router;